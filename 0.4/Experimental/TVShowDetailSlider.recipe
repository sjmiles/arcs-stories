import 'https://$cdn/artifacts/Common/Detail.manifest'

//----------------------------------
// Generic (Slideup) Detail Renderer
//----------------------------------

recipe DetailSlider
  use #selected as selected
  DetailSlider
    selected = selected
    consume modal as modalSlot
      provide content as contentSlot
