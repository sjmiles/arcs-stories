import 'https://$cdn/artifacts/Common/Detail.manifest'

//---------------------------------
// Generic Popup-Detail Renderer
//---------------------------------
recipe DetailSlider
  use as selected
  DetailSlider
    selected = selected
    consume modal as modalSlot
      provide content as contentSlot
    description `display information on ${DetailSlider.selected}`
